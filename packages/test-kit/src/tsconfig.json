{
  "extends": "../../../tsconfig.base.json",
  "compilerOptions": {
    "module": "commonjs",
    "outDir": "../cjs",
    "types": [
      "node", // needed for setTimeout, although we could have used lib: "dom", "es2017" as well
      "mocha" // contract uses mocha functions
    ]
  }
}