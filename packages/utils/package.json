{
  "name": "@file-services/utils",
  "description": "Common file system utility functions.",
  "version": "0.0.1",
  "main": "cjs/index.js",
  "module": "esm/index.js",
  "types": "esm/index.d.ts",
  "scripts": {
    "clean": "rimraf ./cjs ./esm",
    "build": "run-p build:cjs build:esm",
    "build:cjs": "tsc --project src --outDir cjs --module commonjs",
    "build:esm": "tsc --project src --outDir esm --module esnext",
    "lint": "run-p lint:src",
    "lint:src": "tslint --project src"
  },
  "dependencies": {
    "@file-services/types": "^0.0.1"
  },
  "devDependencies": {
    "@file-services/test-kit": "^0.0.1"
  },
  "files": [
    "cjs",
    "esm",
    "src"
  ],
  "license": "MIT",
  "repository": "git@github.com:wixplosives/file-services.git",
  "publishConfig": {
    "access": "public"
  }
}
